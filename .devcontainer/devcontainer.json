{
	"dockerComposeFile": "docker-compose.yml",
	"service": "app",
	"workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
	// "forwardPorts": [8000]
	// "postCreateCommand": ["pip install fastapi", "pip install "uvicorn[standard]"", "uvicorn main:app --reload"]
}
